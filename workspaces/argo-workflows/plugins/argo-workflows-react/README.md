# @backstage-community/plugin-argo-workflows-react

Web library for the argo-workflows plugin.

This package contains reusable React components, hooks, and utilities for visualizing Argo Workflows data in Backstage.
